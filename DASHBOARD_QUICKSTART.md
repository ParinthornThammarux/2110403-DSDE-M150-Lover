# 🚀 Dashboard Quick Start Guide

## วิธีการรัน Dashboard แบบรวดเร็ว

### ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
pip install -r visualization/dashboard/requirements.txt
```

หรือติดตั้งแบบแยก:

```bash
pip install streamlit pandas numpy plotly folium streamlit-folium scikit-learn joblib
```

### ขั้นตอนที่ 2: รัน Dashboard

จาก root directory ของโปรเจค:

```bash
streamlit run visualization/dashboard/main_dashboard.py
```

### ขั้นตอนที่ 3: เปิดเว็บเบราว์เซอร์

Dashboard จะเปิดอัตโนมัติที่: `http://localhost:8501`

---

## 📋 ตรวจสอบก่อนรัน

รันคำสั่งนี้เพื่อตรวจสอบว่าไฟล์ครบถ้วน:

```bash
cd visualization/dashboard
python test_structure.py
```

ควรเห็นผลลัพธ์:
```
[OK] main_dashboard.py
[OK] viz_modules.py
[OK] ml_integration.py
[OK] clean_data.csv
[OK] rf_forecaster.pkl
[OK] anomaly_if_model.pkl
```

---

## 🎯 Features ที่สำคัญ

### 1. กราฟที่ร้องขอ
- ✅ แต่ละเขตมี complaint อะไรบ้าง จำนวนเท่าไหร่ (มี filter)
- ✅ แต่ละ complaint มีในเขตไหนบ้าง เขตละเท่าไหร่ (มี filter)
- ✅ เขตไหน complaint เยอะสุด

### 2. ML Models
- ✅ RandomForest Forecaster - พยากรณ์จำนวน complaint
- ✅ Isolation Forest - ตรวจจับความผิดปกติ

### 3. Visualizations เพิ่มเติม
- ✅ แผนที่ Heat Map และ Marker Clusters
- ✅ Heatmap ความเข้มตามเวลา
- ✅ รูปแบบตามช่วงเวลาและวัน
- ✅ สถิติและการวิเคราะห์ต่างๆ

---

## 📊 ข้อมูลที่ใช้

- **Data:** clean_data.csv (780,000+ rows)
- **ML Models:**
  - `ml_models/forecasting/models/rf_forecaster.pkl`
  - `ml_models/anomaly_detection/anomaly_if_model.pkl`

---

## 🎛️ การใช้งาน Filters

### Sidebar
1. **เลือกช่วงเวลา** - กรองตามวันที่
2. **เลือกเขต** - กรองเฉพาะเขต
3. **เลือกประเภท** - กรองเฉพาะประเภท complaint
4. **ประเภทแผนที่** - Heat Map หรือ Clusters

### ในแต่ละกราฟ
- กราฟแต่ละเขตมี complaint อะไรบ้าง: มี filter เลือกประเภทเฉพาะ
- กราฟแต่ละ complaint มีในเขตไหน: มี filter เลือกเขตเฉพาะ
- เปรียบเทียบแนวโน้ม: เลือกหลายเขตพร้อมกันได้

---

## ⚠️ หากเจอปัญหา

### Dashboard ไม่เปิด
```bash
# ตรวจสอบว่าติดตั้ง streamlit แล้ว
streamlit --version

# ลองรันจาก dashboard directory
cd visualization/dashboard
streamlit run main_dashboard.py
```

### ไม่พบ clean_data.csv
- ตรวจสอบว่าไฟล์อยู่ใน root directory
- ต้องรัน streamlit จาก root directory

### ML Models ไม่โหลด
- ไม่ต้องกังวล! ระบบจะใช้วิธีสำรอง:
  - Forecasting: ใช้ historical patterns
  - Anomaly: ใช้วิธีทางสถิติ

---

## 📝 โครงสร้างไฟล์

```
2110403-DSDE-M150-Lover/
├── clean_data.csv                          # ข้อมูลหลัก
├── ml_models/
│   ├── forecasting/models/
│   │   └── rf_forecaster.pkl              # RandomForest model
│   └── anomaly_detection/
│       └── anomaly_if_model.pkl           # Isolation Forest model
└── visualization/dashboard/
    ├── main_dashboard.py                   # ไฟล์หลัก (รันไฟล์นี้)
    ├── viz_modules.py                      # Visualization functions
    ├── ml_integration.py                   # ML integration
    ├── requirements.txt                    # Dependencies
    └── README.md                           # เอกสารฉบับเต็ม
```

---

## 🎓 คำอธิบายแต่ละ Tab

1. **🗺️ แผนที่ภูมิศาสตร์** - ดูการกระจายตัวบนแผนที่
2. **📊 วิเคราะห์เขตและประเภท** - กราฟหลักที่ร้องขอทั้งหมด
3. **🤖 ML: การพยากรณ์** - พยากรณ์ด้วย RandomForest
4. **⚠️ ML: ความผิดปกติ** - ตรวจจับด้วย Isolation Forest
5. **📈 การวิเคราะห์เพิ่มเติม** - รูปแบบตามเวลาและสถิติ

---

**DSDE M150-Lover Team | Chulalongkorn University**
